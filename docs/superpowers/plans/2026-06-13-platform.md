# tainted-spring-platform Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로컬에서 모든 마이크로서비스가 공유할 인프라(MySQL, PostgreSQL, Redis, Kafka, Zookeeper)를 Docker Compose로 띄우고, 헬스체크와 스모크 테스트로 검증 가능한 `tainted-spring-platform` 레포를 만든다.

**Architecture:** 멀티레포 구성의 오케스트레이션/인프라 전용 레포. 애플리케이션 코드·계약(스키마)을 일절 보유하지 않는다. Compose가 인프라 컨테이너를 기동하고, 각 데이터스토어에 서비스별 DB/스키마를 init 스크립트로 미리 생성한다. 추후 서비스 컨테이너를 더할 수 있도록 "에이전트 부착 슬롯"(빈 `JAVA_TOOL_OPTIONS`)을 가진 서비스 템플릿 앵커와 k8s 매니페스트 스켈레톤 디렉토리를 둔다.

**Tech Stack:** Docker Compose v2, confluentinc/cp-kafka + cp-zookeeper 7.6.1, mysql:8.4, postgres:16, redis:7-alpine, bash 스모크 스크립트.

---

## 워크스페이스 규약 (모든 계획 공통)

- **워크스페이스 루트**: `/Users/changjoonbaek/workspace_claude-code/` — 모든 레포가 이 아래 형제(sibling)로 존재한다.
- **문서 허브**: `tainted-spring-msa/` (현재 디렉토리) — specs/plans 문서만 보관. 서비스 레포가 아니다.
- **이 계획이 만드는 레포**: `/Users/changjoonbaek/workspace_claude-code/tainted-spring-platform/`
- 아래 모든 파일 경로는 이 레포 루트 기준 상대경로다.

## 데이터스토어 할당 (전 서비스 공통 참조표)

| 엔진 | 인스턴스 | 서비스별 DB/스키마 | 네트워크 호스트 | 호스트 포트 |
|---|---|---|---|---|
| MySQL 8.4 | 단일 | `authuser`, `community` | `mysql:3306` | 3306 |
| PostgreSQL 16 | 단일 | `diary`, `mindgraph`, `analytics` | `postgres:5432` | 5432 |
| Redis 7 | 단일 | 논리 DB index: 0=auth, 1=mindgraph, 2=counseling, 3=notification | `redis:6379` | 6379 |
| Kafka 7.6.1 | 단일 브로커 | 토픽 자동생성 ON | `kafka:29092`(내부) / `localhost:9092`(호스트) | 9092 |
| Zookeeper 7.6.1 | 단일 | — | `zookeeper:2181` | 2181 |

## 사전 설치 요구사항

- Docker Desktop (Compose v2.1+ — `docker compose up --wait` 사용)
- Bash, Git
- 애플리케이션 빌드 없음 — 이 레포는 인프라/오케스트레이션만 담당(로컬 JDK/Maven 불필요).

## File Structure

- `README.md` — 레포 목적, 사용법(`make up`, `make smoke`, `make down`)
- `.gitignore`
- `.env.example` — 크리덴셜/포트 기본값
- `docker-compose.yml` — 인프라 5종 + Kafka UI(profile `ui`) + 서비스 템플릿 앵커
- `init/mysql/01-create-databases.sql` — `authuser`, `community` DB 생성
- `init/postgres/01-create-databases.sql` — `diary`, `mindgraph`, `analytics` DB 생성
- `scripts/smoke.sh` — 5종 인프라 연결성 검증 (이 레포의 "테스트")
- `Makefile` — `up` / `down` / `smoke` / `logs` 단축 명령
- `k8s/README.md` + `k8s/00-namespace.yaml` — EKS 매니페스트 스켈레톤(서비스별 매니페스트는 각 서비스 계획에서 추가)

---

## Task 1: 레포 스캐폴드

**Files:**
- Create: `/Users/changjoonbaek/workspace_claude-code/tainted-spring-platform/.gitignore`
- Create: `/Users/changjoonbaek/workspace_claude-code/tainted-spring-platform/README.md`

- [ ] **Step 1: 레포 디렉토리 생성 및 git 초기화**

Run:
```bash
mkdir -p /Users/changjoonbaek/workspace_claude-code/tainted-spring-platform
cd /Users/changjoonbaek/workspace_claude-code/tainted-spring-platform
git init
```
Expected: `Initialized empty Git repository ...`

- [ ] **Step 2: `.gitignore` 작성**

`.gitignore`:
```gitignore
.env
*.log
.DS_Store
```

- [ ] **Step 3: `README.md` 작성**

`README.md`:
```markdown
# tainted-spring-platform

tainted-spring-msa 테스트베드의 로컬 인프라/오케스트레이션 레포.
애플리케이션 코드·계약은 보유하지 않는다.

## 사용법
\`\`\`bash
cp .env.example .env
make up      # 인프라 기동
make smoke   # 인프라 연결성 검증
make down    # 정리
\`\`\`

## 제공 인프라
MySQL 8.4 / PostgreSQL 16 / Redis 7 / Kafka 7.6.1 / Zookeeper 7.6.1
\`\`\`
```

- [ ] **Step 4: 커밋**

```bash
git add .gitignore README.md
git commit -m "chore: scaffold platform repo"
```

---

## Task 2: 스모크 테스트 스크립트 (실패하는 테스트 먼저)

**Files:**
- Create: `scripts/smoke.sh`
- Create: `Makefile`

- [ ] **Step 1: 스모크 스크립트 작성**

`scripts/smoke.sh`:
```bash
#!/usr/bin/env bash
# 5종 인프라가 실제로 응답하는지 docker compose exec 로 검증한다.
set -euo pipefail

fail() { echo "SMOKE FAIL: $1"; exit 1; }

echo "[1/5] zookeeper..."
# cub(Confluent Utility Belt)는 cp-* 이미지에 기본 포함 — nc 등 외부 바이너리에 의존하지 않음.
docker compose exec -T zookeeper cub zk-ready localhost:2181 10 || fail "zookeeper"

echo "[2/5] kafka..."
docker compose exec -T kafka kafka-topics --bootstrap-server localhost:9092 --list >/dev/null || fail "kafka"

echo "[3/5] mysql..."
docker compose exec -T mysql mysqladmin ping -h 127.0.0.1 --silent || fail "mysql"
docker compose exec -T mysql sh -c 'mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -e "SHOW DATABASES;"' | grep -q authuser || fail "mysql db authuser"
docker compose exec -T mysql sh -c 'mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -e "SHOW DATABASES;"' | grep -q community || fail "mysql db community"

echo "[4/5] postgres..."
docker compose exec -T postgres pg_isready -U postgres >/dev/null || fail "postgres"
for db in diary mindgraph analytics; do
  docker compose exec -T postgres psql -U postgres -lqt | cut -d'|' -f1 | grep -qw "$db" || fail "postgres db $db"
done

echo "[5/5] redis..."
docker compose exec -T redis redis-cli ping | grep -q PONG || fail "redis"

echo "SMOKE OK: all infrastructure healthy"
```

- [ ] **Step 2: 실행권한 부여 + Makefile 작성**

Run: `chmod +x scripts/smoke.sh`

`Makefile`:
```makefile
.PHONY: up down smoke logs
up:
	docker compose up -d --wait
down:
	docker compose down -v
smoke:
	./scripts/smoke.sh
logs:
	docker compose logs -f
```

- [ ] **Step 3: 테스트가 실패하는지 확인 (아직 compose 없음)**

Run: `make smoke`
Expected: FAIL — `no configuration file provided` 또는 `SMOKE FAIL: ...` (compose 파일/컨테이너가 없어서 실패). 이것이 정상이다.

- [ ] **Step 4: 커밋**

```bash
git add scripts/smoke.sh Makefile
git commit -m "test: add infrastructure smoke test (failing)"
```

---

## Task 3: Compose — Zookeeper + Kafka

**Files:**
- Create: `docker-compose.yml`

- [ ] **Step 1: compose 파일에 zookeeper/kafka 작성**

`docker-compose.yml`:
```yaml
name: tainted-spring-platform

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.1
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
      ZOOKEEPER_4LW_COMMANDS_WHITELIST: "ruok,srvr,conf,stat"
    ports:
      - "2181:2181"
    healthcheck:
      # cub zk-ready 는 cp-zookeeper 이미지에 기본 포함(cp-base-new 유래). nc 미사용.
      test: ["CMD", "cub", "zk-ready", "localhost:2181", "10"]
      interval: 10s
      timeout: 15s
      retries: 10

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    depends_on:
      zookeeper:
        condition: service_healthy
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    ports:
      - "9092:9092"
    healthcheck:
      test: ["CMD", "kafka-topics", "--bootstrap-server", "localhost:9092", "--list"]
      interval: 10s
      timeout: 10s
      retries: 15
```

- [ ] **Step 2: 기동 후 kafka/zookeeper 헬스 확인**

Run:
```bash
docker compose up -d --wait zookeeper kafka
docker compose ps
```
Expected: `--wait` 가 두 컨테이너의 헬스체크 통과까지 블록한 뒤 종료. `docker compose ps` 에서 `zookeeper`·`kafka` 모두 `(healthy)`.

- [ ] **Step 3: 커밋**

```bash
git add docker-compose.yml
git commit -m "feat: add zookeeper and kafka to compose"
```

---

## Task 4: Compose — MySQL + PostgreSQL + Redis + init 스크립트

**Files:**
- Modify: `docker-compose.yml` (서비스 추가)
- Create: `init/mysql/01-create-databases.sql`
- Create: `init/postgres/01-create-databases.sql`

- [ ] **Step 1: MySQL init SQL 작성**

`init/mysql/01-create-databases.sql`:
```sql
CREATE DATABASE IF NOT EXISTS authuser CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS community CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

- [ ] **Step 2: PostgreSQL init SQL 작성**

`init/postgres/01-create-databases.sql`:
```sql
CREATE DATABASE diary;
CREATE DATABASE mindgraph;
CREATE DATABASE analytics;
```

- [ ] **Step 3: compose에 datastore 3종 추가**

`docker-compose.yml` 에 추가한다. `mysql`/`postgres`/`redis` 블록은 기존 `services:` 맵 안에(= `kafka` 와 같은 들여쓰기) 넣고, 마지막 `volumes:` 블록은 **파일 최상위**(= `services:` 와 같은 들여쓰기, `services:` 바깥)에 둔다:
```yaml
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpw}
    ports:
      - "3306:3306"
    volumes:
      - ./init/mysql:/docker-entrypoint-initdb.d:ro
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "--silent"]
      interval: 10s
      timeout: 5s
      retries: 15

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgrespw}
    ports:
      - "5432:5432"
    volumes:
      - ./init/postgres:/docker-entrypoint-initdb.d:ro
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 15

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  mysql-data:
  postgres-data:
```

- [ ] **Step 4: 커밋**

```bash
git add docker-compose.yml init/
git commit -m "feat: add mysql, postgres, redis with init databases"
```

---

## Task 5: 환경변수 + Kafka UI(선택) + 서비스 템플릿 앵커

**Files:**
- Create: `.env.example`
- Modify: `docker-compose.yml`

- [ ] **Step 1: `.env.example` 작성**

`.env.example`:
```dotenv
MYSQL_ROOT_PASSWORD=rootpw
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespw
```

- [ ] **Step 2: compose 상단에 서비스 템플릿 앵커 추가 (추후 서비스가 재사용)**

`docker-compose.yml`의 `name:` 줄 바로 아래에 추가:
```yaml
# 추후 추가될 각 Spring 서비스는 이 앵커를 재사용한다.
# JAVA_TOOL_OPTIONS 는 비워 둔다 — 품질 도구가 -javaagent 등을 주입할 슬롯.
x-java-service: &java-service
  environment:
    JAVA_TOOL_OPTIONS: "${JAVA_TOOL_OPTIONS:-}"
    SPRING_PROFILES_ACTIVE: "docker"
  restart: unless-stopped
```

- [ ] **Step 3: Kafka UI를 profile `ui`로 추가 (기본 미기동)**

`docker-compose.yml`의 `services:` 아래에 추가:
```yaml
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    profiles: ["ui"]
    depends_on:
      kafka:
        condition: service_healthy
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
    ports:
      - "8080:8080"
```

- [ ] **Step 4: 커밋**

```bash
git add .env.example docker-compose.yml
git commit -m "feat: add env defaults, java-service anchor, optional kafka-ui"
```

---

## Task 6: 전체 기동 + 스모크 테스트 통과

- [ ] **Step 1: 환경파일 준비 후 전체 기동**

Run:
```bash
cp .env.example .env
make up
docker compose ps
```
Expected: `make up`(`up -d --wait`)이 5개 컨테이너 헬스체크 통과까지 블록한 뒤 종료. `docker compose ps` 에서 `zookeeper kafka mysql postgres redis` 모두 `(healthy)`.

- [ ] **Step 2: 스모크 테스트 실행 (이제 통과해야 함)**

Run: `make smoke`
Expected:
```
[1/5] zookeeper...
[2/5] kafka...
[3/5] mysql...
[4/5] postgres...
[5/5] redis...
SMOKE OK: all infrastructure healthy
```

- [ ] **Step 3: 정리 검증 후 커밋**

Run: `make down`
Expected: 컨테이너·볼륨 제거 성공.

```bash
git add -A
git commit -m "test: full stack passes smoke test" --allow-empty
```

---

## Task 7: k8s 스켈레톤

**Files:**
- Create: `k8s/00-namespace.yaml`
- Create: `k8s/README.md`

- [ ] **Step 1: 네임스페이스 매니페스트 작성**

`k8s/00-namespace.yaml`:
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: tainted-spring
```

- [ ] **Step 2: k8s README 작성**

`k8s/README.md`:
```markdown
# EKS 매니페스트 스켈레톤

서비스별 Deployment/Service/ConfigMap/Secret 매니페스트는 각 서비스 레포의
구현 계획에서 이 디렉토리에 추가된다. 모든 리소스는 `tainted-spring` 네임스페이스에 배포.
서비스 디스커버리는 k8s Service DNS(예: `http://tainted-spring-diary`)를 사용한다.
```

- [ ] **Step 3: 커밋**

```bash
git add k8s/
git commit -m "feat: add k8s namespace skeleton"
```

---

## Self-Review

**Spec coverage (설계문서 §5.4, §5.5 대비):**
- MySQL/Postgres/Redis/Kafka/Zookeeper 5종 → Task 3·4 ✔
- 서비스별 DB 분리(authuser/community, diary/mindgraph/analytics) → Task 4 init SQL ✔
- 빈 `JAVA_TOOL_OPTIONS` 슬롯 → Task 5 앵커 ✔
- Kafka UI 선택 → Task 5 profile `ui` ✔
- `.env` 외부화 → Task 5 ✔
- k8s 스켈레톤 → Task 7 ✔
- Redis 논리 DB index 할당 → 데이터스토어 할당표에 명시(서비스 계획에서 참조) ✔

**Placeholder scan:** 모든 step에 실제 파일 내용/명령/기대출력 포함. 플레이스홀더 없음. ✔

**Type/이름 일관성:** 네트워크 호스트명(`kafka:29092`, `mysql:3306` 등)·DB명·Redis index가 데이터스토어 할당표와 compose/init 스크립트에서 일치. ✔
